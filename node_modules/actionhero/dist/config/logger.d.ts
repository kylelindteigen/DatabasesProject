export declare const DEFAULT: {
    logger: (config: any) => {
        loggers: any[];
        maxLogStringLength: number;
    };
};
export declare const test: {
    logger: (config: any) => {
        loggers: any[];
    };
};
