import { Connection } from "../classes/connection";
export declare namespace i18n {
    function determineConnectionLocale(connection: Connection): any;
    function invokeConnectionLocale(connection: Connection): void;
    /**
     * Return a translated string.
     */
    function localize(message: string | Array<string>, object?: Connection | any): any;
}
